# API URL - zmień na adres IP swojej karty WiFi w Windows
# Znajdź adres: ipconfig w Windows CMD -> "Wireless LAN adapter Wi-Fi" -> "IPv4 Address"
# Przykład: 192.168.1.100, 192.168.0.150, 10.0.0.25

# Dla lokalnego developmentu (tylko na tym samym komputerze):
# VITE_API_URL=http://localhost:8000/api

# Dla dostępu z sieci WiFi (telefon, tablet, inne urządzenia):
# Zamień 192.168.1.100 na TWÓJ adres IP WiFi z Windows!
# PingMe - Przykładowa konfiguracja API

# Zmień ten adres na adres IP, pod którym działa Twój backend Symfony.
# Możesz go znaleźć, wpisując `hostname -I` w terminalu WSL.
VUE_APP_API_URL=http://TWOJ_ADRES_IP:8000/api
